---
- name: Undo 04 clean Grid metadata before removing Oracle paths
  ansible.builtin.import_tasks: cleanup_grid_install_state.yml

- name: Undo 04 remove Oracle path directories
  ansible.builtin.shell: rm -rf "{{ item }}"
  args:
    executable: /bin/bash
  loop:
    - "{{ grid_home }}"
    - "{{ oracle_inventory }}"
    - "{{ oracle_base }}"
  failed_when: false
  changed_when: true
